<Window x:Class="GOINSP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Height="{Binding SystemParameters.PrimaryScreenHeight}" 
        Width="{Binding SystemParameters.PrimaryScreenWidth}"
        DataContext="{Binding Account, Source={StaticResource Locator}}"
        WindowStartupLocation="CenterScreen" WindowState="Maximized">

    <Grid>
        <Image Source="Resources/GoInsp.jpg" Height="200" Width="200" VerticalAlignment="Top" Margin="60"></Image>

        <StackPanel Height="80" Width="400">
            <WrapPanel>
                <TextBlock FontSize="18" FontStyle="Oblique" Width="150">Gebruikersnaam:</TextBlock>
                <TextBox FontSize="18" HorizontalAlignment="Left" Height="30" Width="250" Text="{Binding LoginName}"></TextBox>
            </WrapPanel>
            <WrapPanel>
                <TextBlock FontSize="18" FontStyle="Oblique" Width="150">Wachtwoord:</TextBlock>
                <PasswordBox FontSize="18" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="250"/>
            </WrapPanel>
            <Button Height="24" Width="100" HorizontalAlignment="Center" Command="{Binding LoginCommand}">Login</Button>
            

        </StackPanel>

    </Grid>
</Window>
