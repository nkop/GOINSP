<Window x:Class="GOINSP.ManagementInformationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sparrow="http://sparrowtoolkit.codeplex.com/wpf"
        Title="GoInsp - Management Informatie" Height="350" Width="525"
        DataContext="{Binding ManagInfoVM, Source={StaticResource Locator}}"
        WindowStartupLocation="CenterScreen" WindowState="Maximized"
        Background="#3598dc">
    <Grid>
        <StackPanel>
            <TextBlock FontSize="40" HorizontalAlignment="Center" Margin="0,50,0,0" Text="Inspecties per inspecteur"></TextBlock>
            <WrapPanel Margin="400,0,0,0">                
                <!--<sparrow:SparrowChart Width="500" Height="500" Margin="90,10,0,10" Background="White">
                    <sparrow:SparrowChart.XAxis>
                        <sparrow:LinearXAxis/>
                    </sparrow:SparrowChart.XAxis>
                    <sparrow:SparrowChart.YAxis>
                        <sparrow:LinearYAxis/>
                    </sparrow:SparrowChart.YAxis>
                    <sparrow:LineSeries Points="0,1,1,2,2,3,3,4"/>
                </sparrow:SparrowChart>-->
                
                <DataGrid Margin="50" Name="InspecteurInspectie" ItemsSource="{Binding ChartData}" Visibility="{Binding InspInsp}" CanUserAddRows="False" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn IsReadOnly="True" Header="Inspecteur" Binding="{Binding Inspecteur.UserName}"/>
                        <DataGridTextColumn IsReadOnly="True" Header="Aantal Inspecties" Binding="{Binding Inspecties}" />
                    </DataGrid.Columns>                    
                </DataGrid>

                <DataGrid Margin="50" Name="BedrijfInspectie" ItemsSource="{Binding BedrInspData}" Visibility="{Binding BedrInsp}" CanUserAddRows="False" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn IsReadOnly="True" Header="Bedrijf" Binding="{Binding Bedrijf.Bedrijfsnaam}"/>
                        <DataGridTextColumn IsReadOnly="True" Header="Aantal Inspecties" Binding="{Binding Inspecties}" />
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel>
                   <DataGrid Margin="90"  Name="Inspecteurs"  ItemsSource="{Binding Inspecteurs}" CanUserAddRows="False"  AutoGenerateColumns="False" SelectedItem="{Binding SelectedInspecteur}" HorizontalAlignment="Right" VerticalAlignment="Top" >
                        <DataGrid.Columns>
                            <DataGridTextColumn IsReadOnly="True" Header="Inspecteur" Binding="{Binding UserName}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Button Command="{Binding ClearInspecteurCommand}" Content="Deselecteer Inspecteur"/>
                    <Button Command="{Binding ShowInspCommand}" Content="Inspecties per Inspecteur" />
                    <Button Command="{Binding ShowBedrCommand}" Content="Inspecties per Bedrijf" />
                </StackPanel>               

            </WrapPanel>
        </StackPanel>
        
    </Grid>
</Window>
